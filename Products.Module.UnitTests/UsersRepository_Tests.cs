namespace Products.Module.UnitTests;

public class UsersRepository_Tests
{
    // user functiality has be implicitly tested by integration tests
    // in current scope no tests are required for UserRepository
    // I created this file just to have this message. I would not do this at work. 
    //[Fact]
    public void Placeholder()
    {
        Assert.True(true);
    }
}